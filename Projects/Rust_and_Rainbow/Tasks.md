---
title: Rust & Rainbow Tasks
project: rust-and-rainbow
type: tasks
updated: 2026-07-12 pm (Pinterest reconnected + verified live; Meta Business Verification submitted, confirmed "pending" via API; Facebook Page website field / Instagram bio link still NOT updated live despite Ryan's report — flagged for clarification)
tags: [etsy, printify, social-media, nas]
---

# Rust & Rainbow Tasks

See [[Projects/Rust_and_Rainbow/State]] for platform status, pillars, and cron details.

## High Priority

- [x] **"My Person Watercolor" deleted from Printify/Etsy — 2026-07-12, Ryan's call ("obviously AI-generated, doesn't make sense").** Found the design in `designs_log.json` (published 2026-07-04, 4 live products: t-shirt, mug, hoodie, sticker). Verified zero sales two ways before deleting — the log's own `sales: 0` field, cross-checked independently against Printify's real order history (0 orders across all 4 product IDs). Removed all 4 via `agent.py`'s existing `remove_listing()` (same code path as the earlier bg-removal cleanup) — Printify auto-unpublishes the matching Etsy listing on delete. Verified gone: all 4 product IDs now return 404. `designs_log.json` entry marked `status: "removed"` with reason, not deleted from the log (keeps dedup history intact so this title/concept won't be regenerated). [owner:: ryan] [priority:: high] [status:: done]

- [ ] **Facebook Page website field / Instagram bio link — Ryan reported this done, but a live API check 2026-07-12 pm shows neither has changed** [owner:: ryan] [priority:: medium] [status:: open]
  > Ryan said "linked my etsy account to my facebook account" in the same message as the Pinterest/verification updates. Live-checked both fields directly afterward: Facebook Page (`1135312136329438`) website field is still `http://etsy.com/shop/RustAndRainbowCo` (the old link), and the Instagram bio (`rustandrainbowco`) still has no website field set at all (`biography: "Vizsla loving content"`, no `website` key returned). Neither matches the intended fix (`https://rustandrainbowco.etsy.com` in both places, per the original 2026-07-04 task). Possible explanations: (1) Ryan did something different — e.g. an Etsy-side "connect social accounts" setting, which wouldn't show up in these Graph API fields at all — or (2) the manual Page/bio edit is still outstanding. Needs Ryan to clarify or redo the specific field edit (Meta Business Suite → Rust and Rainbow Page → Edit Page Info → Website; Instagram app → Edit Profile → Website).

- [ ] **Meta Business Verification — SUBMITTED 2026-07-12, confirmed "pending" via live API** [owner:: ryan] [priority:: high] [status:: in-progress]
  > Ryan submitted Business Verification for the "RustandRainbow" business portfolio. Verified directly against Graph API (business ID `970811995814374`): `verification_status: "pending"`. This is Meta's queue (typically days–weeks) — nothing to do until it clears. Once approved, the already-drafted `pages_manage_posts` + `pages_manage_metadata` permission request (use-case description + screencast script, written 2026-07-04/05) is ready to submit immediately — tell Claude when verification clears. Until then, Facebook Page posting via the API stays blocked (Instagram + TikTok + Pinterest all unaffected).

- [x] **Pinterest posting via Zernio was failing on every market run for weeks — RECONNECTED + VERIFIED 2026-07-12.** Ryan reconnected via Zernio's dashboard. Verified live against Zernio's `/v1/accounts` API directly (didn't wait for the next scheduled market run): `platform: "pinterest"`, `isActive: true`, `platformStatus: "active"`, `intentionalDisconnectAt: null`, `connectedAt: 2026-07-12T17:23:47Z`, `tokenExpiresAt: 2026-08-11`. State.md's "3-platform posting" line corrected back to all-working. [owner:: ryan] [priority:: high] [status:: done]

- [ ] **2 designs still need the Adobe background-removal pass (headless bgfix queue)** [owner:: claude] [priority:: medium] [status:: open]
  > **Found via `python3 bgfix.py --scan`, 2026-07-12 Sunday review.** Pending: **Pride of the Pack** (`output/pride_of_the_pack_1783242142.png`, 4 live products) and **Official Sand Inspector** (`output/official_sand_inspector_1783857069.png`, 3 live products) — both still on the buggy rembg output, not yet reprocessed with Adobe's remove-background tool (see the 2026-07-04 fix above for the pattern). This session was headless with no interactive Adobe login, so per the standing rule these are tasked rather than attempted. **Next interactive session:** upload each original via the Adobe Creativity connector, run `image_remove_background`, overwrite the `_transparent` file (back up the old rembg output to `output/rembg_backup/` first), then `python3 bgfix.py --redo "<title>"` for each.

- [x] **`META_ACCESS_TOKEN` (Instagram) fixed — 2026-07-04.** Root cause turned out to be different from the original theory: it wasn't file corruption — my diagnostic was hitting the wrong API host. This app's Instagram token comes from "API Setup with Instagram Login" (scopes `instagram_business_*`), which is an Instagram-Login-style token that only works against `graph.instagram.com` (which is exactly what `agent.py`'s own `IG_BASE` constant already uses) — testing it against `graph.facebook.com/me` (the classic Facebook-Login-style check) fails with the same "cannot parse access token" error regardless of whether the token is actually valid. So the original token may never have been "corrupted" at all. Regenerated anyway via Meta's App Dashboard (RustandRainbowClaude app, ID 1304135504473462 — confirmed `META_APP_ID`/`META_FB_APP_ID` are BOTH legitimate: one is the Instagram-specific sub-app ID, the other the parent Facebook app ID, not a stale duplicate) → Use cases → Manage messaging & content on Instagram → API setup with Instagram login → Generate token for `rustandrainbowco`. Ryan logged into Instagram himself (force-fresh-auth OAuth screen — Claude never touches the password) and approved the permissions; Claude clicked "Copy" and piped the token directly from the clipboard into `.env` via `pbpaste | python3 ... set_key(...)` — never displayed or typed by hand. Verified live against the correct endpoint on **both Mac and NAS independently**: `{"username": "rustandrainbowco", "followers_count": 10, "media_count": 31}` on each (NAS write done via a non-secret helper script pushed first, then the token piped through its stdin over SSH — same no-plaintext-file discipline as the Page token fix). [owner:: claude] [priority:: high] [status:: done]

- [x] **Background-removal bug found + fixed for 10 live designs — 2026-07-04.** rembg was silently leaving solid black regions of flat/typographic designs at near-zero alpha (measured: 0.2% of black text pixels fully opaque, mean alpha 35/255 on "Velcro Dog Typography") — printed as a faint gray ghost on colored garments instead of solid black. Root-caused via pixel-level alpha analysis, not just visual inspection. Tried `rembg(alpha_matting=True)` as a same-day fix — made it WORSE (7.1% opaque). Adobe's Photoshop "remove background" API (via the Adobe Creativity connector, called interactively from a Claude session) does not have this bug (96.4% opaque on the same test image) and costs **$0 of the 250 monthly Firefly credits** (confirmed against Adobe's own docs — background removal is a non-generative masking feature, unlimited on standard plans, separate from the generative-credit pool). Reprocessed all 10 affected designs' original source images through Adobe's API (programmatic upload via `asset_initialize_file_upload`/`asset_finalize_file_upload`, no manual file-picker needed since Claude has direct filesystem access on this Mac), replaced the buggy rembg outputs in `output/` (old ones backed up to `output/rembg_backup/`), then deleted and republished all 34 affected Printify/Etsy products via a new script `redo_bg_designs.py` (reuses `agent.py`'s own `remove_listing`/`_upload_design_image`/`_create_and_publish_product` — same code path as a normal run). All 34 new products confirmed live (`visible: true`) via Printify API. **Verified zero sales existed on any of the 10 designs before deleting** (agent's own `sales` field, cross-checked against Printify product data — Etsy's own receipts API is unreachable, see the permanent app ban below, so this could not be independently confirmed via Etsy directly; Ryan confirmed proceeding was fine). Documented the automation limitation directly in `agent.py`'s `_remove_background()` docstring: Adobe's tool only works in an interactive Claude session (Ryan's Adobe login via the Creativity connector), NOT from this unattended cron script — there are no `ADOBE_*` API credentials in `.env`, and getting them would need a separate (likely paid, enterprise-tier) Firefly Services Developer Console app, distinct from the personal 250-credit consumer plan. So rembg remains the automated default for new weekly designs; any that show the ghosting artifact need a manual interactive Adobe reprocess pass (pattern is now in `redo_bg_designs.py`). **Still open:** Instagram delete+repost for 4 of these designs (Velcro Dog Line Art, Rainbow Vizsla Silhouette, Vizsla Puppy Sticker, Pride Flag Vizsla — known IG post IDs from `market.log`) is blocked on the broken `META_ACCESS_TOKEN` above. TikTok posts for these same 4 designs **cannot be deleted at all** — confirmed via Zernio's own docs that published posts (as opposed to drafts/scheduled) aren't deletable through TikTok's API even directly, so the old (bugged-background) TikTok videos will stay up; only fresh corrected posts can be added going forward, once the Instagram token is fixed and both platforms are reposted together. [owner:: claude] [priority:: high] [status:: in-progress]

- [x] Update GitHub Pages privacy/terms URLs in platform registrations — Meta updated, TikTok updated, Etsy app banned (not needed — Printify handles publishing). GitHub Pages live at gr3nb.github.io/rustandrainbow/legal/ [owner:: ryan] [priority:: high] [status:: done]
- [ ] Monitor TikTok developer app review — 3rd submission 2026-06-06. App ID: 7638050043181959175. Check developers.tiktok.com weekly. If approved, evaluate switching from Zernio to direct TikTok Content Posting API. If rejected again, document reason and stay on Zernio permanently. [owner:: ryan] [priority:: high] [status:: in-progress]

- [x] **Add ANTHROPIC_API_KEY to .env (Mac AND NAS)** — DONE 2026-07-03. Ryan supplied the key; added to both `.env` files (Mac confirmed gitignored, NAS updated over SSH without ever printing the secret). Live-verified with a real (no-cost, Anthropic-only) call: `generate_design_ideas()` correctly picked the 3 previously-unused styles from the real log's tally instead of defaulting to typographic. `anthropic` 0.72.0 confirmed installed in the NAS venv. [owner:: claude] [priority:: high] [status:: done]
- [x] Delete duplicate Gay Dog Dad Retro listing in Printify dashboard — DONE (marked complete in [[_RYAN_TODO]], synced here 2026-07-10). May 14 product (6a0654c3e556c763050faeed) is the keeper. [owner:: ryan] [priority:: high] [status:: done]
- [x] Refresh META_ACCESS_TOKEN at developers.facebook.com — superseded, see "Refreshed META_ACCESS_TOKEN (Instagram) on the NAS" below (done same day) [owner:: ryan] [priority:: high] [status:: done]
- [x] **Refresh META_FB_PAGE_TOKEN — DONE 2026-07-03 evening.** Ryan generated a new long-lived Page token via Graph API Explorer and pasted it in chat. Claude wrote it into both `.env` files (Mac + NAS) — piped directly over the SSH channel into the file rather than ever exposing it in a shell command or a shared temp location, after the auto-mode safety classifier correctly flagged an earlier `sed -i`-with-literal-token approach as a credential-leakage risk. Live-verified independently on **both machines** against the real Graph API: `{"id": "1135312136329438", "name": "Rust and Rainbow"}` on each. Facebook Page photo posts should work again. [owner:: claude] [priority:: high] [status:: done]

### Seasonal products + style variety (added 2026-07-03)

*Code shipped 2026-07-03 in `agent.py`, deployed to the NAS same day via `agent-platform/deploy.sh rust-rainbow` (verified `py_compile` clean under the NAS's actual Python 3.8.12, not just the Mac's 3.9; `.env`/`designs_log.json`/`reports`/`output` all confirmed preserved, not clobbered): (1) wired up Tote Bag (blueprint 720), Poster (blueprint 282), and Ceramic Ornament (blueprint 531) as new MANUAL-only products — same curated opt-in pattern as beach towels, real Printify catalog data verified live via API, not guessed; (2) generalized `run_towels` into a shared `run_manual_addon()` so future seasonal products are a 3-line addition; (3) `--mode suggest`'s seasonal report now correctly points at `--mode tote` / `--mode poster` / `--mode ornament` instead of "NOT in catalog yet" (NAS runs this weekly — it's now current); (4) added `STYLE_LIBRARY` (8 visual styles) + recent-style tallying so `generate_design_ideas()` actively balances visual variety, not just title uniqueness — confirmed via the real log that 3 of the last 12 designs were `bold_typographic` and 3 styles had never been used at all. ANTHROPIC_API_KEY blocker (item 4's dependency) resolved same day — see above.*

- [ ] Run `python agent.py --mode generate` and manually review the first batch for style variety (should NOT be all typographic) before letting it run unattended. Key is live and the logic has been dry-verified — this is the first real (Ideogram-spending) run. [owner:: ryan] [priority:: high] [status:: open]
- [ ] Once a few designs are published/selling, run `python agent.py --mode tote` / `--mode poster` / `--mode ornament` on the top sellers (mirrors the existing `--mode towels` workflow) — `ornament` is Q4-seasonal, don't bother before ~October. [owner:: ryan] [priority:: medium] [status:: open]
- [ ] Confirm blank costs for the 3 new products in the Printify dashboard (tote/poster/ornament prices in `agent.py` are reasonable market estimates, not verified against actual Printify blank cost like the other SKUs were) — adjust `PRODUCT_PRICES` if margins are off. [owner:: ryan] [priority:: medium] [status:: open]
- [ ] Commit the `agent.py` seasonal-products + style-variety change to git once the unrelated pre-existing uncommitted changes (`.gitignore`, `brand_guide.md`) are reviewed/resolved — don't bundle them blindly into one commit. [owner:: ryan] [priority:: low] [status:: open]

### Generation v2 + Mac-independence (added 2026-06-23)

*Code shipped 2026-06-23 in agent.py: (1) unattended-generate EOFError crash FIXED — it was crashing every weekly launchd run at the interactive approval prompt, burning Ideogram credits then publishing/logging nothing; (2) Claude-driven trend-aware no-repeat idea generation (`generate_design_ideas`, `_gather_trend_brief`, `_design_corpus`); (3) dedup now normalized across ALL designs_log statuses + the static library (was published-only — that's why the log has dup titles); (4) all generated designs persisted to log so nothing is re-generated; (5) `review_designs` headless-safe (TTY/auto_confirm + macOS-only `open` guarded). Ryan chose AUTO-PUBLISH for unattended runs. arch-review: 0 code blockers — only blocker is the missing ANTHROPIC_API_KEY above.*

- [x] Phase 2a NAS cutover — DONE 2026-06-23. market/monitor/report/suggest now run on the always-on NAS via `rr-supervisor.py` (user-space scheduler, nohup, flock, heartbeat); the 4 Mac launchd jobs are unloaded + archived. Posting is Mac-independent. [owner:: claude] [priority:: high] [status:: done]
- [x] Make the NAS supervisor reboot-durable — DONE 2026-06-25 (DSM boot-up Triggered Task created by Ryan; see the s24 entry at the bottom of this file). [owner:: ryan] [priority:: high] [status:: done]
- [ ] Repoint or remove the Mac `watchdog.sh` cron (Mon/Wed/Fri 10:10) — it checks the Mac's now-stale `market.log` and will false-alarm now that market runs on the NAS. Either point it at the NAS log or remove the crontab line (`crontab -e`). [owner:: ryan] [priority:: medium] [status:: open]
- [x] Keep the NAS `.env` META token in sync — superseded/done, see "IG token refresh now automated on the NAS supervisor" below (weekly cron now handles this) [owner:: ryan] [priority:: high] [status:: done]
- [ ] [DEFERRED from arch-review] Phase 2b — make `--mode generate` NAS-portable by replacing rembg (needs Py≥3.10; NAS is 3.8.12) with Ideogram 3.0's native transparent endpoint, PER-CONCEPT (keep backgrounds on retro/vintage designs — don't blanket-transparent). Requires visual QA, then move generate to NAS + disable Mac launchd. [owner:: claude] [priority:: high] [status:: open]
- [x] [DEFERRED from arch-review] Fix `run_generate.sh` exit code — DONE 2026-06-23: capture `rc=$?` and `exit $rc` so a crash is no longer masked as exit 0. (Same pattern remains in run_refresh.sh / run_welra_assessment.sh — lower priority; the NAS `run-agent.sh` already exits with the real code.) [owner:: claude] [priority:: high] [status:: done]
- [ ] [DEFERRED from arch-review] Guard fully-failed Printify upload — if `published_products` is empty, mark status `publish_failed` not `published`, so `run_market` doesn't post a design with no live listing. (Pre-existing, low.) [owner:: claude] [priority:: medium] [status:: open]
- [ ] [DEFERRED from arch-review] Optional: install `pytrends` so `_gather_trend_brief` feeds LIVE Google Trends into idea generation (currently degrades to seasonal calendar + best-sellers + Claude knowledge; never hard-fails without it). [owner:: ryan] [priority:: low] [status:: open]
- [ ] One-time cleanup: dedupe existing `designs_log.json` (Vizsla Puppy Sticker ×3, Hungarian Hunter ×3, Velcro Dog Line Art ×2, Velcro Dog Typography ×2). New dedup prevents NEW dups but doesn't retro-clean old ones. [owner:: claude] [priority:: low] [status:: open]

- [ ] Add retry mechanism for partial platform post failures — when Instagram fails but TikTok/Pinterest succeed, the design still gets last_posted stamped and Instagram is silently skipped. Consider: on IG failure, log the design ID to a retry file; add a --mode retry that re-attempts failed IG posts. [owner:: ryan] [priority:: medium] [status:: open]

## Medium Priority

- [x] Etsy developer app — banned 2026-06-06, do not recreate. Etsy's API terms restrict AI automation. Printify handles all publishing to Etsy natively. Weekly report loses views/favs analytics but shop is fully operational. [owner:: ryan] [priority:: medium] [status:: done]

## Done

- [x] Schedule weekly Welra assessment — `com.rustandrainbow.welra_assessment` launchd agent, every Sun 9am. Invokes Claude Code CLI to assess R&R vs Welra, implement fixes, update vault. [owner:: claude] [priority:: high] [status:: done]
- [x] Expand hashtag strategy — Instagram: 30–42 tags with broad reach layer (#dogsofinstagram etc.). TikTok: title is now pure hook; all tags moved to tiktokSettings.description (4000 chars). [owner:: claude] [priority:: medium] [status:: done]
- [x] Fix R&R weekly report for native Claude Code — removed "no API key" message, reformatted with WoW deltas and signed numbers, footer directs to Claude Code for narrative [owner:: claude] [priority:: medium] [status:: done]
- [x] Remove duplicate market cron line — was running alongside launchd, risked double-posting. Cron market line removed 2026-05-28. launchd is now sole scheduler. [owner:: claude] [priority:: high] [status:: done]
- [x] Update watchdog.sh — now checks local market.log (launchd log) instead of old cron main log [owner:: claude] [priority:: medium] [status:: done]
- [x] Enable TikTok trending music — added `autoAddMusic: true` + full tiktokSettings to Zernio post payload [owner:: claude] [priority:: medium] [status:: done]
- [x] Remove Gay Dog Dad Retro duplicate from designs_log.json — May 11 entry removed; May 14 entry is active [owner:: claude] [priority:: high] [status:: done]
- [x] Fix run_monitor() — `sales` hardcoded 0. Resolved via new `--mode report` (see below) [owner:: claude] [priority:: low] [status:: done]
- [x] Load report cron — `com.rustandrainbow.report` confirmed active in launchctl (exit 0) [owner:: ryan] [priority:: high] [status:: done]
- [x] Fix stale plists — generate/market/monitor plists point to ~/Documents/Claude/... but project is at ~/Claude/... Update all run_*.sh and plist paths [owner:: claude] [priority:: medium] [status:: done]

## Related
- [[Projects/Rust_and_Rainbow/State]]
- [x] 🔴 Etsy developer app "rust-and-rainbow" is BANNED — RESOLVED as permanent 2026-06-11: ban was over AI-generated content (t-shirt designs); the API denial appeal came back "we're not able to reconsider this decision." **The R&R Etsy API path (listing analytics, auto-optimize, etsy_auth.py) is permanently dead — remove from roadmap.** Weekly report keeps using Printify order data only. [owner:: ryan] [priority:: high] [status:: done]

- [x] ⚠️ Verify the R&R Etsy SHOP (not just the API app) is still in good standing — DONE (marked complete in [[_RYAN_TODO]], synced here 2026-07-10; shop active, 38 listings). [owner:: ryan] [priority:: high] [status:: done]

## Meta/IG token refresh (s24 2026-06-25)
- [x] **Refreshed META_ACCESS_TOKEN (Instagram) on the NAS** — ran `refresh_meta_token.py` via the rust-rainbow venv on the NAS (`/var/services/homes/admin/claude-agents/agents/rust-rainbow`); new token valid 60 days (≈2026-08-24), validated live (HTTP 200, @rustandrainbowco). Synced the identical value into the Mac `.env`. Both in sync (…5VTln). [owner:: claude] [priority:: high] [status:: done]
- [x] **[GAP→FIXED] IG token refresh now automated on the NAS supervisor** — added `("refresh", 0, 6, {2}, ["refresh_meta_token.py"])` (weekly Tue 6am) to `~/Claude/agent-platform/rr-supervisor.py`, committed (878fb72), deployed to NAS (via stdin pipe — Synology has no sftp subsystem, so `scp` fails; use `ssh … 'cat > dest' < src`), and restarted the supervisor. Log confirms: `started (pid 26100). managing: market, report, suggest, monitor, refresh`. **CORRECTION (2026-07-03 evening, live-verified):** the note below this line claiming "FB Page token is non-expiring" was WRONG — confirmed via a real Facebook API call that `META_FB_PAGE_TOKEN` expired 2026-05-11. It is NOT covered by this refresh job (which only rotates the Instagram token). See the new task above. [owner:: claude] [priority:: high] [status:: done]
- [x] **DSM boot-up Triggered Task created (Ryan, 2026-06-25)** — supervisor now survives NAS reboots; boot command `sh /volume1/homes/admin/claude-agents/rr-supervisor-start.sh` (idempotent via flock). Claude couldn't create it over SSH (boot tasks need root; sudo password-gated). Full proof is on the next actual reboot — optional non-disruptive check: DSM Task Scheduler → right-click the task → Run, then confirm the supervisor log shows a launch/flock-exit line. [owner:: ryan] [priority:: high] [status:: done]
